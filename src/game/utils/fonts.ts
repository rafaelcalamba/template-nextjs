import { Types } from 'phaser';

export const fontStyles = {
    default: {
        fontFamily: 'Inconsolata',
        fontSize: 14,
        color: '#fff',
        stroke: '#000',
        strokeThickness: 0,
        align: 'center'
    } as Types.GameObjects.TextStyle,
};
